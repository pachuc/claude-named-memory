---
description: Install a personal /name shortcut so /named-agents:name can be invoked as just /name
allowed-tools: [Bash]
---

Run `install-shortcut.sh` via Bash and show the user the output verbatim.
