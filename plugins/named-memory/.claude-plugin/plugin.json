{
  "name": "named-memory",
  "description": "Named memory profiles for Claude Code. Minimal by default: /load <name> and /save manage a persistent per-name memory.md with no changes to your shell. Opt into /install-extra for claude-<name> aliases plus automatic load and SessionEnd extraction/compaction.",
  "version": "0.2.0",
  "author": {
    "name": "pachu"
  }
}
