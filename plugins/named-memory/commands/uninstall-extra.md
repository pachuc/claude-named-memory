---
description: Disable the extra automatic experience — remove aliases, shortcuts, and make hooks dormant (memory is kept)
allowed-tools: [Bash]
---

Run `uninstall-extra.sh` via Bash and show the user the output verbatim.
