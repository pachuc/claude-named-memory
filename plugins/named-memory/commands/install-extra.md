---
description: Enable the extra automatic experience — claude-<name> shell aliases, hooks, and /name /load /save shortcuts
allowed-tools: [Bash]
---

Run `install-extra.sh` via Bash and show the user the output verbatim.
